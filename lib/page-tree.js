var mergeTrees = require('broccoli-merge-trees'),
    PageWriter = require('./page-writer'),
    path = require('path'),
    fs = require('fs');

function PageTree(tree) {
  console.log(tree);
};

module.exports = PageTree;
